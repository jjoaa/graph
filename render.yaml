services:
  - type: web
    name: flask-drug-dashboard
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y fonts-nanum
      fc-cache -fv
      pip install -r requirements.txt
    startCommand: python app.py
